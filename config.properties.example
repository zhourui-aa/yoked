# ============================================================
# yoked 微信 AI 机器人 — 配置文件模板
# ============================================================
# 复制为 config.properties 并填入真实 Key
# 优先级：-D参数 > 环境变量 > 此文件
# config.properties 在 .gitignore 中，不会提交到 Git

# -----------------------------------------------------------
# DeepSeek AI 对话（必需）
# 获取：https://platform.deepseek.com/api_keys
# -----------------------------------------------------------
deepseek.api.key=sk-你的DeepSeek密钥
# deepseek.websearch=true             # 可选：原生联网搜索开关（默认 true，模型固定为 deepseek-v4-flash）

# -----------------------------------------------------------
# 和风天气（可选）
# 获取：https://console.qweather.com/
# -----------------------------------------------------------
# qweather.api.key=你的和风天气key
# qweather.api.host=

# -----------------------------------------------------------
# 火山引擎 Ark — 图片生成 Seedream（可选）
# 获取：https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey
# -----------------------------------------------------------
# ark.api.key=你的ark-key

# -----------------------------------------------------------
# 火山引擎 Ark — 图片识别 Doubao Vision（可选）
# -----------------------------------------------------------
# ark.vision.api.key=你的vision-key

# -----------------------------------------------------------
# 阿里云 DashScope — Qwen TTS 语音合成（可选）
# 获取：https://bailian.console.aliyun.com/
# -----------------------------------------------------------
# dashscope.api.key=你的dashscope-key

# -----------------------------------------------------------
# Kiprio 时间查询 API（可选，500次/天免费）
# 获取：https://kiprio.com/signup
# -----------------------------------------------------------
# datetime.api.key=你的kiprio-key

# -----------------------------------------------------------
# 快递鸟 快递查询（可选）
# 获取：https://www.kdniao.com/ 注册后获取
# -----------------------------------------------------------
# kdniao.ebusiness.id=你的商户ID
# kdniao.app.key=你的API Key

# -----------------------------------------------------------
# SQLite 数据库路径（可选，默认 yoked.db）
# -----------------------------------------------------------
# sqlite.db.path=yoked.db

# -----------------------------------------------------------
# 自定义时间 API 地址（填了就不走 Kiprio）
# -----------------------------------------------------------
# datetime.api.url=
