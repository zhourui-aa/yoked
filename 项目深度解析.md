# yoked-main 项目深度解析

> 基于微信 iLink SDK + DeepSeek AI 的多模态智能聊天机器人

---

## 一、项目定位与技术栈

### 1.1 项目定位

`yoked-main` 是一个**微信 AI 聊天机器人**，用户在微信里发消息（文字/语音/图片/文件），机器人通过 DeepSeek 大模型理解意图，按需调用 20+ 种外部能力（天气、新闻、股票、生图、识图、TTS 语音等），最终把整合后的结果回复给用户。核心设计思想是 **Function Calling（函数调用）架构**——AI 不直接知道天气怎么查，但能"决定调用哪个工具、传什么参数、如何把多个工具结果整合成一段回复"。

### 1.2 技术栈

| 维度 | 选型 | 说明 |
|------|------|------|
| 语言/JDK | Java 21 | 使用 record、var、switch 模式等新特性 |
| 构建 | Maven 3.9.x | 无 Spring Boot，纯 SDK 工程 |
| 微信接入 | wechat-ilink-sdk 2.3.3 | `io.github.lith0924:wechat-ilink-sdk`，扫码登录 + 长轮询收消息 |
| AI 对话 | DeepSeek API（OpenAI 兼容） | openai-java 4.27.0 SDK，走 `api.deepseek.com/v1` |
| 生图 | 火山引擎 Ark SDK | 豆包 Seedream 5.0，`doubao-seedream-5-0-pro` |
| 识图 | 火山引擎 Ark | 豆包视觉，`doubao-seed-2-0-lite` |
| 语音合成 | 阿里云 DashScope | Qwen3-TTS，14 种音色 |
| JSON | Jackson + Gson | openai-java 用 Jackson，工具参数用 Gson |
| 文档解析 | PDFBox 3.0 + POI 5.4 | PDF / Word / Excel 文本提取 |
| 天气 | 和风天气 QWeather | 自带 com.weather 子模块封装 |

### 1.3 配置机制

配置通过 `ConfigUtil` 按**三级优先级**查找：系统属性（`-D`参数） > 环境变量 > 项目根目录 `config.properties`。所有需要 API Key 的服务都遵循这套机制，缺失 Key 时服务优雅降级（创建时抛 `IllegalStateException` 被 BotApp 捕获，置 null 后对应工具不注册）。

| 配置项 | 环境变量 | 用途 | 必需 |
|--------|----------|------|------|
| deepseek.api.key | DEEPSEEK_API_KEY | AI 对话 | 是 |
| qweather.api.key + host | QWEATHER_API_KEY / _HOST | 天气 | 否 |
| ark.api.key | ARK_API_KEY | 文生图 | 否 |
| ark.vision.api.key | ARK_VISION_API_KEY | 图片识别 | 否 |
| dashscope.api.key | DASHSCOPE_API_KEY | TTS 语音 | 否 |
| datetime.api.key | DATETIME_API_KEY | 时区查询 | 否 |
| music.api.url | MUSIC_API_URL | 音乐搜索 | 否 |
| serpapi.api.key | SERPAPI_API_KEY | 联网搜索 | 否 |
| kdniao.ebusiness.id/app.key | KDNIAO_* | 快递查询 | 否 |

---

## 二、目录结构总览

```
yoked-main/
├── pom.xml                              # Maven 依赖
├── config.example.properties            # 配置模板
├── src/main/java/
│   ├── com/weather/                     # 【独立子模块】天气查询 CLI
│   │   ├── WeatherApp.java              #   命令行入口（REPL 交互）
│   │   ├── service/WeatherService.java  #   和风天气 API 封装
│   │   ├── model/WeatherData.java        #   天气数据模型
│   │   └── exception/WeatherException.java
│   │
│   └── org/example/bot/                 # 【核心】微信机器人
│       ├── BotApp.java                  #   主程序入口（1028 行，消息路由中枢）
│       ├── Diagnostic.java              #   工具诊断程序
│       │
│       ├── ilink/                       #   接入层
│       │   ├── BotCluster.java          #     多 Bot 集群管理
│       │   └── ILinkBot.java            #     单 Bot 封装（微信 SDK 门面）
│       │
│       ├── model/
│       │   └── BotMessage.java          #   统一消息载体（文字/图片/语音/文件）
│       │
│       ├── tools/                       #   工具系统
│       │   ├── ToolCenter.java          #     工具注册中心 + 动态构建
│       │   ├── ToolDefinition.java      #     单个工具定义（名/描述/参数/执行器/条件）
│       │   ├── ToolCondition.java       #     工具可见性条件（函数式接口）
│       │   └── ToolContributor.java     #     批量工具贡献者（函数式接口）
│       │
│       ├── service/                     #   服务接口层（17 个接口）
│       ├── impl/                        #   服务实现层（19 个实现）
│       │   ├── DeepSeekAiServiceImpl.java   #   ★ 核心 AI 引擎
│       │   ├── Session.java / SessionManager.java  # 会话管理
│       │   ├── BotState.java            #     per-user 状态缓存（图片/文档/新闻）
│       │   └── ...其余 16 个能力实现
│       │
│       └── util/
│           └── ConfigUtil.java          #   三级优先配置读取
```

---

## 三、分层架构详解

项目分为 **5 个层次 + 1 个独立子模块**，自上而下数据流动：

### 第 1 层：接入层 `ilink`

负责与微信打交道，把微信 SDK 的原始消息转换为项目内部统一的 `BotMessage`。

| 类 | 职责 | 关键机制 |
|----|------|----------|
| `BotCluster` | 多微信号 Bot 集群管理 | `CopyOnWriteArrayList<ILinkBot>` 存所有 bot；`CountDownLatch` 等待初始化 bot 全部登录；`ThreadLocal<ILinkBot>` 在 handler 线程中传递"当前处理消息的是哪个 bot"（`current()`）；支持运行时 `addBotDynamic()` 动态新增 |
| `ILinkBot` | 单 Bot 封装，微信 iLink SDK 门面 | 工厂 `create(name)` 配置 35s 超时/3 次重试/30s 心跳；`login()` 扫码登录（二维码过期自动重生成，最多 10 次）；`startPolling()` 守护线程长轮询 `getUpdates()`；收到消息经单线程 `handlerExecutor` 异步处理保证顺序；`tryRelogin()` 会话过期自动重登（最多 5 次）；`toBotMessage()` 把 SDK 的 `WeixinMessage` 转 `BotMessage`，依次识别 voice/image/file/text 四种类型 |

### 第 2 层：消息模型 `model`

| 类 | 职责 |
|----|------|
| `BotMessage` | 微信消息的统一不可变数据载体。私有构造 + 4 个静态工厂方法 `text()/image()/voice()/file()`。提供 `isImage()/isVoice()/isFile()` 类型判断。携带 `userId/text/imageBytes/voiceBytes/voiceText/fileBytes/fileName` 全部字段 |

### 第 3 层：消息处理中枢 `BotApp`（核心入口）

`BotApp` 是整个项目的"大脑调度中心"，`main()` 方法完成启动，内部 `setHandler` 注册的消息处理器是所有消息的统一入口。

**启动流程（`main` 方法三步走）：**
1. **创建 Bot 集群**：`new BotCluster()` + 循环 `addBot()`，每个 bot 在后台线程生成登录二维码等待扫码
2. **创建所有服务**：逐个实例化 20+ 个服务（带 Key 的服务用 try-catch 包裹，缺失则置 null 优雅降级），调用 `registerAllTools()` 把全部能力注册进 `ToolCenter`
3. **注册消息处理器 + 阻塞等待**：`cluster.setHandler(msg -> {...})` 注册统一处理器，`awaitLogins()` 阻塞等所有 bot 登录完成，之后主线程 `join()` 阻塞，消息由 handler 线程处理

**消息处理器（handler）的分流逻辑：**
```
收到 BotMessage
  ├─ isVoice  → handleVoice()   语音转文字 → processTextMessage(forceVoice=true)
  ├─ isImage  → handleImage()   视觉识别 + 缓存图片 + 记入对话历史
  ├─ isFile   → handleFile()    提取文本(TXT/PDF/Word/Excel) → AI 总结 + 缓存文档
  └─ 文字     → processTextMessage()
```

**`processTextMessage` 的五级路由（核心）：**

| 级别 | 处理 | 说明 |
|------|------|------|
| ① 本地命令 | `tryHandleLocalCommand()` | 精确/前缀匹配，零 API 消耗。支持：帮助、设定人设、开启/关闭语音模式、切换音色、查看音色库、新建bot |
| ② 语音意图 | 关键词命中 | 文本含"语音/说话/讲给我"等词，或来自语音消息（forceVoice），标记 `wantsVoice` |
| ③ 构建工具列表 | `buildTools()` | 委托 `ToolCenter.buildTools()` 按当前用户状态动态构建可用工具 + 条件过滤；额外直接注册 `play_music`（需访问 bot 发文件） |
| ④ Function Calling | `ai.chatWithTools()` | 一次 API 调用，AI 自主决定调用哪个工具、传什么参数，最多循环 5 轮执行工具并回传结果 |
| ⑤ 降级自由对话 | `ai.chat()` | FC 无结果时走纯对话 |

回复统一用 `sendTextWithTyping`（带"正在输入"状态），语音模式/关键词命中时额外用 `sendAsVoice()` 调 TTS 合成音频发送。

### 第 4 层：AI 引擎 + 工具系统

#### AI 引擎 `DeepSeekAiServiceImpl`（impl 层，核心中的核心）

| 方法 | 作用 |
|------|------|
| `chat(userId, userMessage)` | 多轮自由对话。从 SessionManager 取该用户历史 → 拼 system prompt → 调 DeepSeek → 写回历史 |
| `chatWithTools(userId, userMessage, tools, executors)` | **最复杂方法**。① 构建 system prompt + 历史 + 工具定义 ② 调 AI 检测 `tool_calls` ③ 循环最多 5 轮：取出工具名 → 从 executors map 找执行器 → 执行拿结果 → 把结果作为 tool 消息回传 AI → AI 决定继续调工具还是出文本 ④ 返回最终文本，无文本时返回 null 触发降级 |
| `record(userId, in, out)` | 手动注入对话历史（用于"图片已识别""图片已生成"等非对话交互后让 AI 知道发生了什么） |
| `setPersona(userId, persona)` | 修改用户人设 |

模型用 `deepseek-v4-pro`，历史上限 `MAX_HISTORY=200` 轮。

#### 工具系统 `tools` 包

这是项目把"20+ 种能力"统一管理的设计，解耦了"工具定义"与"工具在何时可见"：

| 类 | 职责 | 设计要点 |
|----|------|----------|
| `ToolDefinition` | 单个工具的完整定义 | 封装 name/description/parameters(JSON Schema)/executor(执行器 lambda)/condition。`isAvailable(userId)` 判断可见性。不可变（Collections.unmodifiableMap） |
| `ToolCondition` | 工具可见性条件 | `@FunctionalInterface`，`test(userId)` 返回是否注册到当前请求。典型用途：新闻服务是否可用、用户是否有图片缓存、是否有文档缓存 |
| `ToolContributor` | 批量工具贡献者 | `@FunctionalInterface`，服务实现此接口即可批量注册自己的工具（当前 BotApp 仍用集中式 `registerAllTools`，此接口为未来解耦预留） |
| `ToolCenter` | 工具注册中心 | `register()` 链式注册；`buildTools(tools, executors, userId)` 每次请求时调用——先 `ThreadLocal` set userId，遍历 registry 跳过 `isAvailable=false` 的，去重后追加；用完 `finally` remove ThreadLocal。`summary()` 生成启动日志 |

**条件工具的精妙之处**：`ask_about_image`（图片追问）和 `ask_about_document`（文档追问）只在用户**有对应缓存**时才注册，避免 AI 在没有图片时"幻觉"调用。这是 per-request 评估，由 `ToolCondition` 在 `buildTools` 时动态判断。

### 第 5 层：服务能力层 `service` + `impl`

采用**接口/实现分离**，17 个接口定义能力边界，19 个实现对接具体外部 API。

#### AI 多模态类

| 接口 | 实现 | 能力 | 外部依赖 |
|------|------|------|----------|
| `AiService` | `DeepSeekAiServiceImpl` | AI 对话 + Function Calling 调度 | DeepSeek API |
| `VisionService` | `DoubaoVisionServiceImpl` | 图片理解，`analyze(bytes, prompt)` | 火山引擎 Ark |
| `ImageGenService` | `SeedreamImageServiceImpl` | 文生图，`generate(prompt) → byte[]` | 火山引擎 Seedream |
| `SpeechService` | `QwenTtsSpeechServiceImpl` | 文字转语音 + 14 音色管理 | 阿里云 DashScope |

#### 金融计算类（`CalculatorServiceImpl` 纯本地 + 1 个实时汇率 API）

| 工具 | 方法 | 实现要点 |
|------|------|----------|
| 复利终值 | `compoundInterest(p,r,y,t)` | BigDecimal 精确，公式 P×(1+r/t)^(t×y) |
| 房贷月供 | `mortgage(loan,rate,years,method)` | 等额本息/等额本金按月循环 |
| 个税 | `calculateTax(salary,ins,ded)` | 2024 七级累进税率表硬编码 |
| 汇率 | `convertCurrency(amount,from,to)` | exchangerate-api.com，缓存 5 分钟 |

`FinanceServiceImpl` 对接新浪财经（A股 CSV）、天天基金（JSONP）、Binance（加密货币 24h ticker），全部免费无 Key。

#### 生活资讯类

| 接口 | 实现 | 能力 |
|------|------|------|
| `WeatherBotService` | (实体类) | 和风天气查询，工厂 `create()` 加载配置 |
| `NewsService` | `RssNewsServiceImpl` | 零 Key 纯 RSS，8 类新闻（中国新闻网 + IT之家），XML 解析带 XXE 防护 |
| `ExpressService` | `ExpressServiceImpl` | 快递鸟 API，自动识别快递公司，14 家内置映射，顺丰需手机后四位 |
| `WebReaderService` | `WebReaderServiceImpl` | 网页正文抓取（微信公众号 js_content 特殊处理）+ AI 摘要 |
| `WebSearchService` | `WebSearchServiceImpl` | SerpAPI Google 搜索，5 分钟缓存 |
| `MusicService` | `MusicServiceImpl` | NeteaseCloudMusicApi，搜索→取URL→下载MP3 发送 |
| `DateTimeService` | `DateTimeServiceImpl` | Kiprio 时区 API，40+ 中文城市名→IANA 映射 |

#### 数据娱乐类

| 接口 | 实现 | 能力 |
|------|------|------|
| `FootballService` | `FootballServiceImpl` | 英超积分榜/赛程/轮次 + 懂球帝新闻搜索。多级缓存：内存30min→本地文件→jsDelivr CDN→GitHub Raw |
| `IdiomService` | `IdiomServiceImpl` | 成语接龙，300+ 词典，首字索引 `Map<Character,List<String>>`，per-user GameState |
| `RandomService` | `RandomServiceImpl` | 骰子/随机数/抽签/硬币，基于 ThreadLocalRandom |
| `DietService` | `DietServiceImpl` | Mifflin-St Jeor 公式算 BMR/TDEE，减脂缺口500/增肌盈余400 |

#### 会话与状态管理类

| 类 | 职责 |
|----|------|
| `Session` | 单个对话会话数据模型，FIFO `trim(maxHistory)` 截断历史 |
| `SessionManager` | userId → Map<会话名, Session>。创建/切换/删除/列出会话；人设管理；语音模式开关；`fullSystemPrompt()` 拼 persona + 技术指令 |
| `BotState` | 线程安全 per-user 缓存（图片/文档/新闻），TTL 5 分钟，`ConcurrentHashMap` + `CacheEntry.expired()`。支持图片追问、文档追问、新闻详情的条件判断 |

#### 基础设施

| 类 | 职责 |
|----|------|
| `ConfigUtil` | 三级优先配置查找（系统属性 > 环境变量 > config.properties） |

### 独立子模块 `com.weather`

`WeatherApp` 是一个完全独立的命令行天气查询工具（REPL 交互式），有自己的 main 方法，支持 help/version/status/weather 命令。它被核心模块的 `WeatherBotService` 复用（通过 `com.weather.service.WeatherService`）。属于"先有 CLI 工具，后被集成进机器人"的演进产物。

### 诊断工具 `Diagnostic`

独立的 main 类，逐一测试每个服务（DeepSeek/天气/生图/识图/TTS/日期时间）的可用状态，打印配置加载情况（Key 脱敏显示）。用于排查"某个能力为什么没生效"。

---

## 四、项目运作完整流程

以用户发一句"北京明天天气怎么样，顺便帮我画一只看星星的猫"为例：

### 阶段 1：消息接收
1. `ILinkBot` 守护线程长轮询 `getUpdates()` 收到微信原始消息
2. `toBotMessage()` 转为 `BotMessage`（文字类型）
3. `BotCluster` 的统一 handler 被触发，`ThreadLocal` 记录当前 bot

### 阶段 2：消息分流
4. handler 判断：不是语音/图片/文件 → 走 `processTextMessage()`

### 阶段 3：本地命令检查
5. `tryHandleLocalCommand()` 精确/前缀匹配"帮助/人设/语音模式/新建bot"等 → 不匹配，返回 false

### 阶段 4：工具构建
6. 检测语音意图 → 无关键词，`wantsVoice=false`
7. `buildTools()` → `ToolCenter.buildTools(tools, executors, userId)`：遍历 registry，跳过条件不满足的（如用户无图片缓存则 `ask_about_image` 不注册），追加约 20 个可用工具 + `play_music`
8. 构建好的 `List<FunctionDefinition>` 和 `Map<工具名, 执行器>` 传给 AI

### 阶段 5：Function Calling 多轮（核心）
9. `ai.chatWithTools()` 调用 DeepSeek，把用户消息 + 历史 + 全部工具定义一起发
10. AI 返回 `tool_calls`：先调 `get_weather(city="北京")` → 执行器返回北京天气 JSON → 作为 tool 结果回传 AI
11. AI 再次决策：调 `generate_image(prompt="一只看星星的猫")` → 执行器把生图任务提交到 `IMAGE_EXECUTOR` 线程池（异步，立即返回"生成中"提示）→ 结果回传 AI
12. AI 拿到天气数据 + 生图已启动的提示，整合成一段自然语言回复："北京明天多云，气温…；猫咪图片正在生成，稍后发你"

### 阶段 6：回复
13. `bot.sendTextWithTyping(userId, 回复, 500)` 发送文字（带"正在输入"500ms 延迟模拟真人）
14. 生图线程异步完成 → `bot.sendImage()` 直接发图片
15. 非 TTS 模式，结束

### 语音消息的特殊路径
语音消息 → `handleVoice()` → SDK 已转文字（`voiceText`）→ 走 `processTextMessage(forceVoice=true)` → 回复时额外 `sendAsVoice()` 调 Qwen TTS 合成音频 → `bot.sendFile()` 发 wav 文件。`cleanForTts()` 会清理括号语气词、emoji 代码、Markdown 标记，保留 `~` 影响语调。

### 图片消息路径
图片 → `handleImage()` → `vision.analyze(imageBytes, prompt)` 豆包识图 → 结果记入对话历史（`ai.record`）+ 缓存到 `BotState`（供后续 `ask_about_image` 追问）→ 回复 + "可继续追问"提示

### 文件消息路径
文件 → `handleFile()` → 依次尝试 readTextFile（UTF-8/GBK/GB2312 多编码，替换字符占比<5% 算成功）/ readPdf（PDFBox 最多 20 页）/ readDocx（POI Word）/ readXlsx（POI Excel）→ 截断 8000 字 → `ai.chat` 总结 → 缓存到 `BotState`（供 `ask_about_document` 追问）

---

## 五、关键设计亮点

1. **Function Calling 架构**：AI 不硬编码"天气怎么查"，而是自主决定调用哪个工具。新增能力只需 `toolCenter.register(new ToolDefinition(...))` 一行，AI 自动学会使用。这是项目可扩展性的根基。

2. **条件工具的防幻觉设计**：图片追问/文档追问只在用户有缓存时才注册，避免 AI 在无图时调用产生幻觉。`ToolCondition` 实现 per-request 动态评估。

3. **优雅降级**：所有可选服务（生图/识图/TTS/搜索/快递）创建时 try-catch，缺 Key 置 null，对应工具不注册。机器人永远能启动，只是能力少一些。

4. **多 Bot 集群**：`BotCluster` + `ThreadLocal` 支持 N 个微信号同时在线，运行时还能通过"新建bot xxx"命令动态扩容。

5. **异步不阻塞**：生图/音乐下载等耗时操作提交到 `IMAGE_EXECUTOR` 单线程池，立即返回"生成中"提示，完成后异步发图片，不阻塞主消息循环。

6. **会话隔离**：每个 userId 独立的对话历史 + 多会话切换 + 独立人设，一个用户的对话不影响另一个。

7. **三级配置 + 环境变量优先**：既适合本地开发（config.properties），也适合生产部署（环境变量注入），且支持 -D 临时覆盖。

8. **文档解析多格式 + 多编码**：TXT 尝试 UTF-8/GBK/GB2312 并按替换字符占比判断；PDF/Word/Excel 分别用对应库；智能适配中文环境。

---

## 六、潜在改进点

1. **BotApp 过载**：1028 行的 `BotApp` 承担了启动、消息路由、4 种类型处理、工具注册、文件解析、TTS 清理等全部职责，可按 `ToolContributor` 接口把各服务的工具注册下沉到服务自身，进一步解耦。
2. **强类型转换**：`botState(ai)`、`((DeepSeekAiServiceImpl) ai).getSessionManager()` 等多处把 `AiService` 强转为具体实现类，耦合了接口与实现，可考虑在 `AiService` 接口补全这些方法。
3. **错误处理**：部分服务实现里 `catch (Exception ignored) {}` 吞掉异常，排查问题时不易定位。
4. **测试缺失**：项目无单元测试，`Diagnostic` 是手动诊断而非自动化测试。
5. **并发安全**：`IdiomServiceImpl` 的 per-user `GameState` 用普通 `HashMap`，多线程下有风险（虽微信消息单线程处理暂时掩盖了问题）。

---

*本解析基于 yoked-main 项目 wechatTest 分支，分析时间 2026-07-25。*
